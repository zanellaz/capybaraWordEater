import styled from "styled-components"

export const S = {
    Wrapper: styled.div`
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: Center;
        flex-wrap: wrap;
        width: 100%;
        text-align: justify;
    `
}